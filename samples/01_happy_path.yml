name: Happy path
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4
      - name: Build project
        run: cargo build
      - name: Run tests
        run: cargo test
