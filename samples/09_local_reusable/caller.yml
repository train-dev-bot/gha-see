name: Local reusable caller
on:
  workflow_dispatch:

jobs:
  call-tests:
    uses: ./reusable.yml
    with:
      suite: smoke

  summarize:
    needs: call-tests
    runs-on: ubuntu-latest
    steps:
      - name: Summarize reusable result
        run: echo "Reusable workflow completed"
